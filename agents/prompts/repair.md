Rewrite the supplied mission.py so it satisfies the request and every concrete
evaluator failure. Return only complete Python source. Use the CubeSat SDK for
hardware, use capture.filename, and keep mission-created files under outputs/.
Do not import subprocess, socket, requests, httpx, urllib, Picamera2, or
RPi.GPIO. Do not use cv2.VideoCapture.
